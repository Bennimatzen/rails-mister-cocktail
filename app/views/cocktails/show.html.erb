<h2> <%= @cocktail.name %> </h2>
<ul>

</ul>

<% @cocktail.doses.each do |dose| %>
<%= dose.description %> <%= dose.ingredient.name %> |||||| <%= link_to "Delete that dose", dose_path(dose), method: :delete,
              data: { confirm: "Are you sure?" } %>
<% end %>

<%= link_to "Go back to your cocktails", cocktails_path %> |||||

<%= link_to "Dose your Cocktail", new_cocktail_dose_path(@cocktail) %> ||||||

<%= link_to "Delete that cocktail", cocktails_path %>


